# IPTV Panel Docker Environment Configuration
#
# Copy this file to .env and fill in the values.
# These variables will be used by docker-compose.

# --- Domain & SSL ---
# The domain for the panel. Must point to the server's IP.
PANEL_DOMAIN=panel.yourdomain.com
# Email for Let's Encrypt SSL certificate notifications.
CERTBOT_EMAIL=youremail@yourdomain.com

# --- Database Settings ---
# These will be used to create the PostgreSQL database and user.
DB_NAME=iptv_panel
DB_USER=iptv_admin
DB_PASS=changeme_db_password # CHANGE THIS to a strong, random password

# --- Application Settings ---
# Secret key for Flask sessions. Generate with 'openssl rand -hex 32'.
SECRET_KEY=changeme_flask_secret_key # CHANGE THIS
# Default admin password for the web UI.
ADMIN_PASSWORD=changeme_admin_password # CHANGE THIS
# API token for scripts like user_manager.sh. Generate with 'openssl rand -hex 32'.
ADMIN_API_TOKEN=changeme_api_token # CHANGE THIS

# --- Optional: Streaming Server & Cloudflare ---
# These can be configured later in the panel's UI.
STREAM_DOMAIN=stream.yourdomain.com
STREAM_SERVER_IP=1.2.3.4
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
STREAMING_API_BASE_URL=
STREAMING_API_TOKEN=
